source :rubygems

gem 'vault-tools'
gem 'redis'
gem 'rest-client'
gem 'thin'
gem 'smart_env', :require => 'smart_env/auto'
gem 'sinatra-google-auth', :require => false
gem 'mail'
gem 'rack-ssl-enforcer'
gem 'scrolls'

group :test do
  gem 'vault-test-tools'
end
